spring:
  application:
    name: rpm
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

dubbo:
  cloud:
   subscribed-services: common-params
  protocols:
    dubbo:
      port: -1
server:
  port: 8099